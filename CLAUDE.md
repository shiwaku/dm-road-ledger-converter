# dm-road-ledger-converter

## プロジェクト概要

道路台帳平面図のDMデータ（`*.dm` / `*.DM`）をGeoJSONに変換するツール。
線・面・記号・方向・注記の5種類に分割して出力する。

都市計画基本図向けの [dm-converter](https://github.com/shiwaku/dm-converter) の派生。
道路台帳は地図情報レベル500で、座標値の単位や含まれる地物が基本図と異なるため分離している。

## もっとも重要な注意点

**座標オフセットの単位はミリメートル（`/1000`）** で、dm-converter のセンチメートル（`/100`）と異なる。
入れ替えて使うと座標が10倍ずれる。単位を変更するときは、必ず図郭サイズと
座標オフセットの実測で裏を取ること（README「dm-converter との違い」に実測表がある）。

## ディレクトリ構成

```
dm-road-ledger-converter/
├── src/
│   ├── index.js          エントリポイント（引数解釈・逐次実行）
│   ├── convert.js        変換ループ（逐次とワーカーで共用）
│   ├── parallel.js       ワーカーへの分割と断片の連結
│   ├── worker.js         並列変換のワーカー
│   ├── dm.js             DMファイルの解析
│   ├── dmfiles.js        .dm ファイルの再帰検索
│   ├── geojsonWriter.js  GeoJSON出力
│   └── epsgDefs.js       平面直角座標系の定義
├── scripts/
│   ├── build.sh                  GeoJSON + GeoParquet + PMTiles の一括生成
│   ├── check-extended-codes.mjs  拡張コードの点検（build.sh が変換直後に実行）
│   ├── standard-codes.mjs        標準コード表の読み口（標準か拡張かの判定）
│   ├── reconcile-pdf.py          PDF図面と変換結果の全数照合（地物の有無）
│   ├── measure-pdf.py            PDF図面の実測（線幅・破線・記号の大きさ・字高）
│   ├── raster-diff.py            ビューワの描画結果とPDF図面のラスタ差分（インクの内訳）
│   └── geojson2parquet.py        GeoParquet変換（ogr2ogr が使えない環境向け）
├── docs/
│   ├── 検証データ調査.md
│   └── 豊中市DM500入手手順.md
├── viewer/               PMTiles を表示するWebビューワ（MapLibre GL JS + Vite）
│   ├── src/main.ts           地図初期化・パネルUI・イベント配線
│   ├── src/layers.ts         ソース定義・レイヤー定義・ポップアップ
│   ├── src/basemap.ts        背景地図（淡色/標準/写真/白図）とダーク化
│   ├── src/theme.ts          テーマの判定と保存
│   ├── scripts/render-sheet.mjs  図郭と同じ範囲・縮尺・方位でヘッドレス描画（raster-diff.py が呼ぶ）
│   └── public/               地理院スタイル（pale/std）・PWA・複製されたタイル
├── data/                 検証用データ（.gitignore）
└── output/               変換結果（.gitignore）
```

ビューワは dm-converter の `viewer/` に揃えている。UI・テーマ・背景地図・PWA だけでなく、
**色（白図＝黒線／ダークで明色）・記号スプライト（dm-sprite）・角度の読み替え（符号反転）も同じ**。
向こうを直したらこちらにも同じ修正が要るか確認する。逆も同じ。

揃えていないのは3点。**いずれも「PDF図面を実測して図面に寄せる」方針からくる差**で、
根拠の数値は `python3 scripts/measure-pdf.py` で出し直せる。

1. 線・面の線幅・破線は公共測量標準図式の20パターンではなく、**同梱PDF図面の実測**で決める
   （`viewer/src/layers.ts` の `LINE_STYLES`）。道路台帳図の分類コードが自治体ごとに
   違うため、図式の書物を持ってきても合わない（Issue #10）
2. **記号の大きさをコードごとに補正しない。** 向こうは bbox の小さいアイコンを
   18.56px へ引き上げているが、こちらは全コード一律 0.5倍（`ICON_SCALE`）。
   ラスタを引き伸ばすと輪郭がぼやけるうえ（`3401` 門は3.0倍で明確にぼけていた）、
   倍率1.0でも図面の1.8〜2.2倍あった（Issue #13、dm-sprite#15）
3. **アイコンが無いコードは記号・方向とも丸で描く。矢印を使わない。**
   方向要素（E6）は「向きを持つ記号」で流向ではないため矢印は誤読を招く。
   代替図形に回る全コードをPDFから切り出したところ、`4181`（8件、四角）と
   `4214`（7件、旗）以外はすべて丸か点だった（その2コードもいまはアイコンで描く。
   `4214` は豊中市の区画、`4181` は代替表で `dm-2235`）

分類コード名称の対応表 `viewer/src/dmCodes.ts` は **dm-converter の同名ファイルの複製**。
図式は共通なので、どちらかを直したらもう一方にも同じ修正を入れる。
豊中サンプルの105コード中77コードをカバーし（件数では5,825件中4,965件＝85.2%）、
標準図式に無い自治体固有コードは「（標準図式に記載なし）」と出す。

**「標準図式にあるか」の判定に名称表を使ってはいけない。** 根拠は dm-sprite の
`data/standard-codes.csv`（453コード。標準か拡張かの機械判定のために起こされた表）だけ。
`dmCodes.ts`（435件）と dm-sprite の `data/symbols.csv`（372件）はどちらも欠けがある。
準則 付録7 の[標準図式PDF](https://www.gsi.go.jp/common/000258741.pdf)を直に引くこともできるが、
**大分類がグループの縦中央に1回だけ印字される**ため行との対応を誤りやすい
（「直前の大分類を引き継ぐ」と `7521 ブレークライン` が `7321` に化ける）。
点検は `npm run check:extended`。

**名称表とスプライトは別々に育つので `cd viewer && npm run check:codes` で整合を確かめる。**
アイコンがあるのに名称が無いコードがあると「アイコンは出るのに記載なしと出る」中途半端な
状態になる。dm-sprite は随時更新されるため（このリポジトリの作業中にも158→159→173に増えた）、
**アイコン数をドキュメントに書かずスクリプトで数える**。
**落とすのは標準図式のコードだけ。** 拡張コードは図式に名称が無いので名称表に足しようがなく、
アイコンが増えるたびに落ちて直しようのない指示を出すことになる（豊中市の区画が入った時点で
実際に8コードが該当した）。標準か拡張かは `scripts/standard-codes.mjs` で判定する。
2026年9月6日時点で名称待ちは0コード。直す場所は dm-converter 側で、複製はそのあと。

加えて、記号・方向はスプライトにアイコンが無いコードを丸で描く。豊中サンプルでは
記号1,210件中1件（`8132` の1コード）だけがこちらに回り、方向1,473件は全件アイコンで描く
（既定の `ext1,toyonaka` の場合。`VITE_DM_PROVIDERS=` で空にすると記号221件／11コード、
方向246件／9コードに増える）。dm-sprite が189コードを持つ時点の数で、**スプライトが
増えると変わるので、書き換えるときは数え直すこと**。数え方は `sprite.json` の
`dm-` で始まるキー。
**スプライトが増えるとこちらは無修正で追随する。** `2235` 雨水桝（347件）が代替図形から
外れ、2026年8月15日には豊中市の区画7コード（`4145` 100件・`4134` 21件・`4133` 14件・
`4214` 7件・`4144` 4件・`2225`・`2248`）が加わって、代替図形が380件から232件に減った。
残っていた `4143`（119件）・`2224`（102件）・`4181`（8件）・`4146`・`4147` は dm-sprite が
「既存アイコンと同形なので追加しない」と決めたコードで、2026年8月28日に `ICON_ALIASES`
（代替表）へ足して232件から1件になった（PR #28）。**スプライトの追加を待つコードと
代替表で解決するコードは別物**で、後者はこちらで判断して書く。

## 使い方

```bash
npm install

# ../DMデータ/ を再帰検索して変換
node src/index.js

# 入力フォルダと座標系を指定
node src/index.js --epsg 6674 --input /path/to/dm_dir

# 逐次実行（並列との一致確認に使う）
node src/index.js --jobs 1 --input /path/to/dm_dir

# GeoJSON + GeoParquet + PMTiles を一括生成
EPSG=6674 scripts/build.sh /path/to/dm_dir
```

縮尺は指定しない。Mレコードから自動取得し `Scale` 属性に入れる。

## 検証用データ

| データ | 場所 | 用途 |
|---|---|---|
| 豊中市サンプル（図郭57-08） | `data/` | 道路台帳の実データ。**シェープファイル版が同梱**され突き合わせできる。EPSG:6674 |
| 静岡市 都市計画基本図 | `../dm-converter/input/` | dm-converter 用。**単位がセンチメートルなので本ツールでは正しく変換できない** |

豊中市サンプルは[市の公開ページ](https://www.city.toyonaka.osaka.jp/machi/doro/daityou/digitalmap/sanple.html)から無償で入手できる。

**検証は `data/` の豊中市サンプルだけで行う。** 誰でも同じ手順で入手でき、変換結果を公開できる
データに限る。これ以外のDMデータは持ち込まず、そこから得た件数・統計・座標もコードと
ドキュメントに載せない（入手先の整理は [docs/検証データ調査.md](docs/検証データ調査.md)）。

**豊中市サンプルの使用は承認済み**（測量成果使用承認書 豊基管第304号、2026年8月27日）。
条件は「成果品に **測量法に基づく豊中市長承認（使用）R8 豊基管第304号** を見やすいところに
明示する」「Web公開時はURLを報告する」。文言は承認書の指定なので**言い換えない**。入っている
場所は ビューワの帰属コントロールと脚注（`layers.ts` の `DATA_ATTRIBUTION`、既定値）、
PMTiles のメタデータ（`build.sh` の `ATTRIBUTION`）、README。**既定は同梱データに合わせて
あるので、別のデータを表示・配布するときは `VITE_DM_ATTRIBUTION` を上書きするか空にする**
（`VITE_DM_PROVIDERS` と同じ考え方）。承認書の原本はリポジトリに置かない（`.gitignore`）。

## 変更時に必ず確認すること

1. **逐次と並列がバイト単位で一致するか**
   ```bash
   node src/index.js --jobs 1 --input <dir> && cp output/*.geojson /tmp/seq/
   node src/index.js --jobs 4 --input <dir> && cp output/*.geojson /tmp/par/
   for f in /tmp/seq/*.geojson; do cmp "$f" "/tmp/par/$(basename "$f")"; done
   ```
2. **豊中サンプルの件数が変わっていないか** — 線2,647／面400／記号1,210／方向1,473／注記95
3. **拡張コードの点検** — `npm run check:extended`（`scripts/build.sh` は変換直後に自動で走る）。
   豊中サンプルは28コード・860件（14.8%）が標準図式に無い。**混ざること自体は普通のことで
   異常ではない。** 終了コード1になるのは「拡張DMのアイコンがあるのに提供元を指定していない」
   場合だけで、これは静かに丸へ落ちるので見た目からは気づけない
4. **シェープファイル版との突き合わせ** — `data/.../SHP_5708/shp` と分類コード単位で比較する
5. **PDF図面との全数照合** — `python3 scripts/reconcile-pdf.py` が地物の有無を両方向で数える。
   豊中サンプルでは描画パス16,737本中16,659本を説明でき、残り78本は
   ヘアライン45本（0.06pt以下＝紙に出ない）・図郭の枠線1本・**DMに無い太線32本
   （1.02pt・227.7m。国道176号沿いの道路区域線に見えるもの）**。
   **延長の比率で語らないこと。** 図郭を貫くヘアラインだけで延長の2割近くを占め、
   実際の食い違いが埋もれる。線幅で分けて数える。
   **ビューワが `Elev` から描く派生ラベルも同じスクリプトが数える。** 等高線の標高と
   標高点の標高は注記（E7）ではないので、注記の照合には現れない。**変換結果が正しくても
   候補が図面より多ければ描きすぎの元になる**（等高線はラベル候補152件に
   対し図面17か所で8.9倍。標高点は図面329か所に対しビューワ262件で、差の67件は
   `7311` にDMの標高が無いぶん）。**ただしこれは「画面に何個出るか」ではない。**
   等高線のラベルは `text-allow-overlap: false` で衝突判定に任せてあり、実数はズームで
   変わる（標高点は `text-allow-overlap: true` なので候補がそのまま全件出る）。
   **画面上の数は 6 のラスタ差分で測る。**
   図面は等高線を全角整数・標高点を半角で描き分けており、これがそのまま判別に使える
6. **PDF図面とのラスタ差分（描いた結果）** — `python3 scripts/raster-diff.py` が
   ビューワを図郭と同じ範囲・縮尺・方位でヘッドレス描画し、PDFの同じ範囲と重ねて数える。
   **有無の照合も実測も「指定値」しか見ていない。** 衝突判定で間引かれるラベル、
   縦書き、線幅が実際にどう出ているかは、描いたものを測らないと分からない。
   豊中サンプルでは5m角マスの一致率94.9%（4,800マス中 両方4,071／ビューワだけ126／
   図面だけ91）。インクはビューワ294,811px 対 図面186,565px で**1.58倍**。
   `--breakdown` が線・記号・文字に分ける（ビューワは `render-sheet.mjs --only` で
   種類ごとに描き直し、PDFは描画パスを `Shape` で再描画。`--reuse` で前回のPNGを使う）。
   **1.58倍はほぼ「測った縮尺」の問題。** 4px/m は zoom 17.97 で、地上サイズ固定の
   頭打ち（z19未満は z19 の値）により記号・文字・線幅が約2倍の地上サイズで出ていた。
   `--ppm 8.157`（z19）で測ると 合計0.95倍・線0.97・文字1.04・記号1.29。
   z19 で残るのは **記号の線の太さ（1.29倍。大きさは合っている）** と
   **等高線ラベルの数（画面に73、図面19。Issue #9）**。4px/m の内訳は
   線1.42・記号4.17・文字2.93 で、これを「描きすぎ」と読んではいけない
7. **PDF図面との突き合わせ（描き方）** — `python3 scripts/measure-pdf.py` が線幅・破線・記号の
   大きさ・字高を実測して出す。`data/.../PDF_57-08/DM-_57-08.pdf` はA0のベクタPDFで、
   テキストもパスも座標つきで取り出せる。図枠のグリッドラベル（`-48,800`〜`-48,400`／
   `-135,000`〜`-135,300`）から PDF pt → 平面直角座標のアフィン変換が作れる
   （実測 5.6687 pt/m ＝ 1:500）。ビューワ側は `map.getBounds()` の範囲を
   そのままPDFの切り出しに渡せば、同じ範囲・同じ縮尺で並べられる。
   **図面は dash 属性を使わず短い実線の連なりで破線を描く**ので、破線パターンは
   描画パスをDMの線に投影して被覆区間を取るしかない

## 実装上の勘所

- **ラスタ差分は落とし穴が6つある。** どれも「絵は一見それらしいのに数字が静かに壊れる」
  種類なので、`scripts/raster-diff.py` と `viewer/scripts/render-sheet.mjs` に
  対処が入っている。
  1. **`fitBounds` では合わない。** 図郭は平面直角座標の矩形だが、Webメルカトルでは
     子午線収差のぶん回った平行四辺形になる。緯度経度の外接矩形に合わせると図郭より
     1.3%広く撮れ、しかも北が0.30度ずれる。中心・ズーム・方位を計算して `jumpTo` する
  2. **`preserveDrawingBuffer` が要る。** MapLibre の既定は false で、
     撮った絵が**真っ白**になる。v5 では `canvasContextAttributes` の下にある
     （直下に書くと型エラー）。常時有効だと遅くなるので `?debug` のときだけ
  3. **`page.screenshot` ではなくキャンバスから読む。** `getCanvas().toDataURL()` なら
     WebGLの中身が確実に取れ、UIが重なる余地も無い
  4. **背景を切り替えると `map.once('idle')` でデータレイヤーを貼り直す。** その idle を
     「描画完了」と取ると、データが乗る前の絵を撮る。描かれた地物数が0でないことまで待つ
  5. **z19 未満で測ると地上サイズ固定の頭打ちが効く。** 既定の 4px/m は zoom 17.97 で、
     記号・文字・線幅が z19 の約2倍の地上サイズで出る。「描きすぎ」を測るときは
     `--ppm 8.157`（z19）で。4px/m は位置の一致（マス）を見る用途に留める
  6. **PDF側を種類ごとに再描画するときは白いパスをマスクとして全種類に割り込ませる。**
     図面は白い塗りで下の線を消してから記号を描く箇所があり、抜くと「消されるはずの線」が
     線の絵に残り（6,125px）、「白の上の記号」が消える。元の並び順（`_seq`）で描く。
     別々に描くと2本の細い線が隣り合う画素が片方ずつでは閾値を越えず、1〜2%減るのは正常
     （全部を1枚に描き直すと182pxまで合う）。4px/m ではPDFの記号（0.3〜0.42ptの細線）が
     1画素未満で、閾値を越える画素が少ないぶん記号の比が大きく出る
- **位置合わせの検算を必ず見ること。** `raster-diff.py` は最良の重なりを与える画素ずれを
  探して出す。**これは補正ではなく検算で、(0,0) 近くに出て初めて他の数字が読める。**
  範囲・縮尺・方位のどこかを間違えると、絵はそれらしいまま数字だけ悪化する
- **vite は `strictPort: true`。** 前の実行の vite が残るとポートを掴んだままで次が
  起動に失敗する。Windows では shell 経由なので `kill()` は cmd.exe までしか届かない。
  `taskkill /T` でプロセスツリーごと落とし、最後に `process.exit(0)` する
- **円（E3）・円弧（E4）・属性（E8）は出力しないが、件数は数えて警告する。** レコード送りは
  `recordcnt + 1` で正しく進むので解析は壊れないが、地物は黙って消える。`dm.js` の
  `skipped` が種別ごとに数え、`convert.js` が集計して `index.js` が最後に出す。
  **同梱の豊中サンプルには1件も入っていないので、警告の動作確認には合成データが要る。**
  実データの `E2` ヘッダの種別2文字を `E4` などに書き換えれば作れる（レコード数は
  そのままで正しく読み飛ばされ、線の件数が書き換えた数だけ減ることで確かめられる）。
  終了コードは変えない。円弧を使うDMも属性を持つDMも正常なデータだから
- **E6（方向）は1要素に複数ペアが入る。** ペアごとに1フィーチャとして出し、`Seq` で区別する。
  先頭ペアだけ読むと豊中サンプルで198本（13.4%）を取りこぼす
- **実データ区分 `6` も三次元**（21バイト/点・4点/レコード）。`3` だけを三次元扱いにしない
- **注記の文字コードは作成元により異なる。** 全角スペース（`0x21`）埋めの痕跡がある場合のみ
  JIS X 0208（7bit）として扱う。推測でJIS判定すると `1?` が `運` になるような誤変換が起きる
- 注記に含まれる `￥`（JIS `216F`）は改行マーカー。`convert.js` の `textOf` で改行（`\n`）に
  置き換えて出す。GeoJSONでは `"光陵\nマンション"` とエスケープされ、MapLibre の
  `text-field` がそのまま2行に描く。豊中サンプルで12件
- `recno` はループ内で進めず、ヘッダ位置から `recordcnt + 1` で確定させる
- **ビューワの文字はグリフに字形があるものしか描けない。** 地理院の最適化ベクトルタイルの
  グリフに入っているフォントは `NotoSansJP-Regular` だけ。`text-font` を明示しないと
  MapLibre 既定の `Open Sans Regular` を要求して404になる。代替矢印も同フォントに字形がある
  `▶`（U+25B6）を使う。`➤`（U+27A4）はグリフ範囲は200で返るが字形が無く、無音で消える
- **スプライトに無い分類コードをそのまま `icon-image` で要求すると地物が黙って消える。**
  道路台帳図には図式に無い自治体固有コードが混ざる。起動時に `sprite.json` を読んで
  アイコンの有無でレイヤーを2つに振り分け、無い側は丸で位置を出す
- **スプライトのキーは `dm-<コード>` だけではない。** 拡張DM（標準図式に無いコード）は
  提供元の区画が挟まる（`dm-ext1-2245`・`dm-ext1-9101100`。dm-sprite#23）。
  **配信されている区画は `ext1`（6コード）と `toyonaka`（7コード。2026年8月15日の
  dm-sprite#30 から）。** 豊中市の区画に `4191` は無い（図面で `4161` と同じ意匠なので
  dm-sprite は作らないと決めた）ため、`4191` は `ICON_ALIASES` で `dm-4161` を指して解決する。
  `toyonaka` は区画と代替表の両方を引き当てるキー。**区画が来たら自動でそちらが勝つ**
  （`loadSpriteIcons()` は 標準 → 区画 → 代替表 の順に採る）ので、
  もし `dm-toyonaka-4191` が来たら `ICON_ALIASES` の `4191` を外す。`npm run check:extended`
  の表示が `dm-4161（toyonaka の代替表）` → `dm-toyonaka-4191（toyonaka の区画）` に変わる
  連結だけで引くと**静かに劣化する**（アイコンが出なくなるが、代替図形に回るので
  エラーにならず気づけない。実際 `dm-2245` の改名で豊中5件が丸に落ちていた）。
  `basemap.ts` の `loadSpriteIcons()` が索引を「分類コード → キー」の対応表にし、
  `layers.ts` の `iconImage()` が区画つきのものだけ `match` で拾う。
  **既定は同梱データ（豊中サンプル）に合わせた `ext1,toyonaka`。** 拡張DMの意匠は
  提供元ごとに違い、表示中のPMTilesがどの提供元のものかはビューワから判定できない。
  **別の自治体のDMを焼いて表示するときは `VITE_DM_PROVIDERS` で上書きするか空にすること**
- **既存アイコンで代替する対応表を持っている**（`basemap.ts` の `ICON_ALIASES`）。
  dm-sprite は「どんなアイコンが存在するか」だけを持ち、**どのコードをどのアイコンで
  代替するかは、データを扱う側の判断**という切り分け（dm-sprite#22）。
  いまは `4191` → `dm-4161`（図面ではどちらも丸囲みの「水」㊌ で、記号としては
  描き分けられていない。大きさは 4191 が 4161 の73%）。**提供元ごとに持つこと**
- **ズーム補間（`interpolate ['zoom']`）を `match` の分岐に入れてはいけない。**
  「Only one zoom-based "step" or "interpolate" subexpression may be used」で
  そのプロパティが丸ごと弾かれ、**レイヤーが無言で消える**。e458b54 の注記 `text-size`
  が実際にこれで、注記95件が1つも描かれていなかった。必ず
  `interpolate(zoom) { 19: match(...), 21: match(...) }` の順で書く
  （`ANNOTATION_SIZE` と `SOLID_WIDTH` が手本）。型チェックは通るので気づけない。
  **見つけ方はコンソールのエラーか `?debug` のHUD**
- **`line-dasharray` は分類コードで切り替えられない**（データ駆動に非対応）。
  破線はパターンごとにレイヤーを分ける（`strokeLayers` が生成する）。線幅は
  データ駆動できるので実線は1レイヤーにまとめてよい。dasharray は線幅の倍数なので、
  線幅を地上サイズ固定にすると破線も自動で地上サイズ固定になる
- **代替図形の丸は `ink.halo`（地色）で抜く。** `ink.fill` は白固定なので、
  ダークテーマだと縁の明色と同じになって塗りつぶした白丸として悪目立ちする
- **面は塗らない**（`fill-opacity: 0`）。PDF図面が塗っていないため。レイヤー自体は
  残してある。クリックで面の属性を拾う当たり判定になっているから
  （`queryRenderedFeatures` は塗りの不透明度を見ない）
- **標高値はEレコードの50〜56桁（ミリメートル）にあり、注記（E7）ではない。**
  同じ位置の意味が分類コードで変わるため、`convert.js` の `ELEV_CODES`（等高線71xx・
  標高点7311/7312）に限って `Elev`（メートル）として出す。`7304`・`7306`（基準点）は
  点番号が入る（DM `3013400` ⇔ SHP版の点番号 `30134`）。`7311` は豊中サンプルでは
  70件すべて0で、DMに標高が無い。**0は未記録として出さない**（出すと標高0が並ぶ）。
  標高点（7311・7312）は小数1桁に揃える（`27000` → `27.0`）。桁を足すだけで丸めない
  （`25730` は `25.73`）。等高線は図面が整数で描くのでそのまま
- **PDF図面はDMより情報が多い。** 同梱の `DM-_57-08.pdf` は `7311` の位置に67個、
  基準点の位置に7個の標高を描くが、**その値はDMに入っていない**（PDFとSHP版だけが持つ）。
  「ビューワに標高が出ない」を変換の不具合と誤診しないこと
- **記号と文字の大きさは地上サイズ固定。** 紙図面は1:500固定なので地上サイズが不変。
  画面ピクセル固定にすると拡大時に図面より小さくなる（標高値の文字は z19 で地上1.35m、
  z20 で0.74m、z21 で0.37m と動いていた。図面は常に0.99m）。`groundSize()` が
  指数2の補間でズーム1段ごとにピクセルを倍にする。**z19 で 1m ＝ 8.157px**
  （緯度34.78度・MapLibreは512pxタイル）。z19未満は端の値で頭打ちになる（補間は z21 まで
  定義してあるが、地図の `maxZoom` は 20 で dm-converter と同じ。z20 まで地上サイズ固定が効く）。
  字高はPDF図面の実測値を使う（標高値0.99m／注記は8114 町丁目名2.25m・8135建物名1.50m・
  8173等高線注記0.75m・8144は0.50m）
- **注記の角度 ±90 は縦書き（縦横フラグ=1）の表現。** 豊中サンプルでは ±90 の注記29件が
  すべてフラグ1だった。符号反転してそのまま回すと横倒しの文字列になるため、基本図ビューワ
  と同じく 0 に倒す。方向（E6）は符号反転だけで倒さない

## 関連

- [dm-converter](https://github.com/shiwaku/dm-converter) — 都市計画基本図向けの本家
- [dm-converter#46](https://github.com/shiwaku/dm-converter/issues/46) — E6の取りこぼしを起票済み
